# pytorch-zarr-loader

## Installation

create env
```bash
mamba create -n pytorch-zarr-loader python=3.10 zarr
mamba activate pytorch-zarr-loader
pip3 install torch --index-url https://download.pytorch.org/whl/cu118 # or visit https://pytorch.org/get-started/locally/
```